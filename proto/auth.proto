syntax = "proto3";

package auth;

service AuthService {
    rpc Authenticate (Authentication) returns (User) {}
}

message Authentication {
    string Authentication = 1;
}

message User {
    string id = 1;
    string username = 2;
    string email = 3;
    string password = 4;
    repeated string role = 5;
}